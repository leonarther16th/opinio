<!DOCTYPE html>
<html>
<head>
  <title>Opinio</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>

  <%= csrf_meta_tags %>
</head>
<body>

<nav class="navbar navbar-default navbar-fixed" role="navigation">
  <div class="container">
  <div class="navbar-header">
    <a class="navbar-brand" href="#"><strong>Opinio</strong></a>
  </div>
    <ul class="nav navbar-nav navbar-right">
        <li><img src=<%="#{current_user.image}?height=200&type=normal&width=200"  if user_signed_in? %> > </li>
        <li><%= link_to("#{current_user.name}", edit_user_registration_path(current_user)) if user_signed_in? %></li>
        <li><%= link_to("",  destroy_user_session_path, :method => 'delete', class: "glyphicon glyphicon-log-in")  if user_signed_in? %></li>
    </ul>

  </div>
</nav>


<div class="container">
<!-- 
	<% if notice %>
		<div class="alert alert-info"><%= notice %></div>
	<% end %>
	<% if alert %>
      	<div class="alert alert-danger"><span class="glyphicon glyphicon-remove"></span>  <%= alert %></div>
    <% end %>
 -->

	<div class="row">
		<div class="col-lg-12">
			<%= yield %>
		</div>
	</div>
</div>

  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
</body>
</html>
